{
  "name": "utc-date",
  "version": "0.0.5",
  "description": "Experimental Date object overwrite for always using UTC dates.",
  "type": "module",
  "main": "load.js",
  "scripts": {
    "start": "node load.js",
    "test": "rm -rf coverage/ && cross-env NODE_OPTIONS=--experimental-vm-modules npx jest --coverage --coverageProvider=v8",
    "coverage": "npm test && cat ./coverage/lcov.info | npx coveralls"
  },
  "author": "jhmaster",
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jhmaster2000/utc-date.git"
  },
  "homepage": "https://www.npmjs.com/package/utc-date",
  "bugs": {
    "url": "https://github.com/jhmaster2000/utc-date/issues"
  },
  "keywords": [
    "utc",
    "date",
    "utcdate",
    "utc-date",
    "gmtdate",
    "gmt-date",
    "gmt",
    "time",
    "datetime"
  ],
  "jest": {
    "transform": {},
    "coverageReporters": [
      "html",
      "text",
      "lcov",
      "lcovonly",
      "json"
    ],
    "collectCoverageFrom": [
      "./{!(cloneObject),}.js",
      "./UTCDate/{!(CalculateTZ|PadMs),}.js",
      "./UTCDate/to/**"
    ]
  },
  "devDependencies": {
    "coveralls": "^3.1.0",
    "cross-env": "^7.0.3",
    "jest": "^26.6.3"
  },
  "engines": {
    "node": ">=12.0.0"
  }
}
